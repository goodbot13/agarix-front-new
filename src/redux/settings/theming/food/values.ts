import { createArray } from 'api/utils';

export const FOOD_SIZE_VALUES = createArray(12, 10, 1);
export const FOOD_GLOW_DISTANCE_VALUES = createArray(12, 30, 15);
export const FOOD_GLOW_STRENGTH_VALUES = createArray(20, 2, 1);
