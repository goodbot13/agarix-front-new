import { createArray } from "api/utils";

export const CELLS_SHADOW_DISTANCE_VALUES = createArray(20, 5, 5);
export const CELLS_SHADOW_STRENGTH_VALUES = createArray(10, 1, 1);
export const CELLS_TRANSPARENCY_VALUES = createArray(51, 0.50, 0.01, 2);