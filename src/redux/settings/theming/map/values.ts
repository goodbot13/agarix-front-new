import { createArray } from "api/utils";

export const MAP_BORDER_ROUNDNESS_VALUES = createArray(10, 0, 15);
export const MAP_BORDER_WIDTH_VALUES = createArray(20, 20, 5);
export const MAP_BORDER_GLOW_DISTANCE_VALUES = createArray(19, 100, 15);
export const MAP_BORDER_GLOW_STRENGTH_VALUES = createArray(20, 3, 1);